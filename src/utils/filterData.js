
const filterData = (data, filteredData, sort) => {
  // const mapData = data.map((tot, index) => {
  //   if(index >= data.length - sort){
  //     tot.tanggal = dateShort(tot.tanggal)
  //     filteredData.push(tot)
  //   }
  // });
};

export default filterData;
